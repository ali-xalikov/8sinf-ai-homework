# Frontend (Vite) sozlamalari — build vaqtida o'qiladi.
# Nusxa oling: .env (lokal) yoki Vercel > Project > Environment Variables (prod).

# Backend manzili. Bo'sh qoldirilsa — nisbiy (bir xil origin) ishlaydi (dev uchun).
# Prodda backend domenini yozing (oxirida "/" bo'lmasin):
# VITE_API_BASE_URL=https://8sinf-ai-backend.onrender.com
VITE_API_BASE_URL=https://8sinf-ai-backend.onrender.com
